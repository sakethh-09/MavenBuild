sudo apt-get update
sudo apt-get install docker.io
sudo apt-get install git
sudo apt-get install nano

Go to GitHub
Copy the repo URL

git clone https://github.com/sakethh-09/aws.git
cd aws
ls

nano Dockerfile

FROM nginx:alpine
COPY . /usr/share/nginx/html

ls

sudo docker build -t saketh .
sudo docker run -d -p 80:80 saketh

NAGIOS DOCKER EXPERIMENT

docker pull jasonrivers/nagios:latest
docker run -d --name saketh -p 8899:80 jasonrivers/nagios:latest

docker ps

Access in browser:
http://localhost:8899

go to public ipv4 address 
Nagios Login Credentials:
Username: nagiosadmin
Password: nagios
